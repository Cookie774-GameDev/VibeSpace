# Execution Prompt — VibeSpace UI Theme Restoration

You are the sole implementation agent for `VS-PR31-UI-THEME-RESTORATION-20260802` in `Cookie774-GameDev/VibeSpace`, branch `agent/pr30-fixes-and-updates`, PR `#31`.

Read `AGENTS.md`, then read every file in:

```text
docs/superpowers/ui-theme-restoration-2026-08-02/
```

Treat those files as the complete operating contract. Use `IMPLEMENTATION_PLAN.md` as the only plan. Do not create additional plans, do not use sub-agents, do not ask for approvals, and do not pause between in-scope non-destructive edits.

## Outcome

Restore MonoChrome, Sakura, Origami/VibeSpace, and Warm/Default to the supplied reference quality; repair global text/card/dialog scaling; replace the incorrect black Codex-chat panel with inline themed execution output; place the transparent token-rate animation in the left chat list/inspector slot; and make the taskbar AI usage module visible, live, efficient, theme-aware, and truthful.

## Required method

1. Perform one fast reference/repository reconnaissance.
2. Fix shared typography, density, dialog, card, tooltip, and portal foundations first.
3. Implement the themes and features in the plan order.
4. Preserve existing functionality and accessibility.
5. Use browser control/visualization at `http://localhost:5173`.
6. Compare screenshots against the supplied references, refine, and repeat until `VISUAL_ACCEPTANCE.md` passes.
7. Use only focused checks; do not spend the goal on a broad test campaign.
8. Use one bounded Tauri/native smoke only for native usage-module visibility/placement.
9. Report exact evidence and limitations. Do not claim anything you did not exercise.

## Hard boundaries

- No unrelated backend or product redesign.
- No fabricated provider usage/quota.
- No duplicate provider setup.
- No new black full-message chat panel.
- No giant global text or card scale.
- No low-resolution stretched art.
- No unintended text over illustrations.
- No broad descendant CSS hacks, mass `!important`, whole-app transforms, or theme-specific component forks.
- No merge, deployment, release publication, destructive changes, or secrets.

Continue until the documented acceptance criteria pass.
