# Pipecat + voice providers
pipecat-ai[anthropic,deepgram,cartesia,groq,silero,livekit,twilio]==0.0.50

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1
mcp==1.12.4

# Twilio
twilio==9.4.1

# LiveKit (Pipecat brings the SDK but we also need the server SDK for room/token mgmt)
livekit-api==0.7.1

# Supabase (for per-user auth, settings, audit)
supabase==2.10.0

# Misc
python-dotenv==1.0.1
pydantic==2.8.2
pydantic-settings==2.7.0
httpx==0.27.2
websockets==13.1
loguru==0.7.3
