/**
 * Projects feature barrel.
 *
 * Re-exports the project pages so PageRouter can import them through
 * a stable surface (`@/features/projects`) without coupling to the
 * exact file layout inside this folder.
 */
export { ProjectDetail } from './ProjectDetail';
